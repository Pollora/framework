<?php

namespace {{ namespace }};

use Pollora\Hook\Contracts\Hooks;
use Pollora\Attributes\{{ hookType }};

class {{ class }} implements Hooks
{
    #[{{ hookType }}('{{ hook }}'{{ priority }})]
    public function {{ hookMethodName }}({{ arg }}){{ returnType }}
    {
        // Code is executed on the "{{ hook }}" WordPress action only.{{ return }}
    }
}
